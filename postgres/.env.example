# Postgres
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=postgres
PG_PORT=5432

# pgAdmin
PGADMIN_EMAIL=admin@local.io
PGADMIN_PASSWORD=
PGADMIN_PORT=5050

# Optional: host folder with SQL dumps you’ll import via pgAdmin’s UI
# On Windows, use an absolute path like C:\Users\you\dev\dumps
# Leave empty to skip mounting
DUMPS_HOST_PATH=